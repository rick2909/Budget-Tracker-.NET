<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="BudgetTracker.App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:BudgetTracker.App"
    xmlns:views="using:BudgetTracker.App.Views"
    BackgroundColor="#1B1A19"
    FlyoutBehavior="Disabled">

    <ShellContent
        ContentTemplate="{DataTemplate views:DashboardView}"
        Route="MainPage" />
    
    <ShellContent
        ContentTemplate="{DataTemplate views:SettingsView}"
        Route="SettingsView" />

    <ShellContent
        ContentTemplate="{DataTemplate views:TransactionsView}"
        Route="TransactionsView" />
</Shell>
